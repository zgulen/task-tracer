import styling from "./tasks.module.css"
import React from 'react'

const Tasks = ({task, daytime}) => {
    console.log(task);
    console.log(daytime);
  return (
    <div>Tasks</div>
  )
}

export default Tasks